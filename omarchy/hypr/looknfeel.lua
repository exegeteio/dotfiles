-- Change the default Omarchy look and feel.

-- For more information, go to https://wiki.hypr.land/Configuring/Basics/Variables/#general
hl.config({
  general = {
    -- Remove the gaps between windows and screen edges.
    gaps_in = 0,
    gaps_out = 0,

    -- Make the window border thinner. The Omarchy default value is 2.
    border_size = 1,
  },
})

-- For more information, go to https://wiki.hypr.land/Configuring/Basics/Variables/#decoration
-- hl.config({
--   decoration = {
--     -- Example: round the window corners.
--     rounding = 8,
--
--     -- Example: dim windows that are not in focus. A value of 0.0 gives no
--     -- dim. A value of 1.0 gives full dim.
--     dim_inactive = true,
--     dim_strength = 0.15,
--   },
-- })

-- For more information, go to https://wiki.hypr.land/Configuring/Basics/Variables/#animations
-- hl.config({
--   animations = {
--     -- Example: turn off all animations.
--     enabled = false,
--   },
-- })

-- For more information, go to https://wiki.hypr.land/Configuring/Basics/Variables/#layout
-- hl.config({
--   layout = {
--     -- Example: prevent a wide single-window layout on a wide screen.
--     single_window_aspect_ratio = { 1, 1 },
--   },
-- })

-- For more information, go to https://wiki.hypr.land/Configuring/Layouts/Scrolling-Layout/
-- hl.config({
--   scrolling = {
--     -- Example: show one column per screen. The default is two columns.
--     column_width = 0.97,
--   },
-- })
