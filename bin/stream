#!/usr/bin/env bash
tmux -L stream new-session -s stream -d
tmux -L stream split-window -v 'dr.wee'
tmux -L stream select-pane -m
tmux -L stream resize-pane -t 1 -y 7
tmux -L stream select-pane -t 0
tmux.re stream
