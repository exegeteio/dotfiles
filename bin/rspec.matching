#!/usr/bin/env bash
specs=$(rg --files spec/**/*_spec.rb "$@")
[ -z "$specs" ] && echo "[rspec.matching] No specs selected!  $specs" && exit 1
echo "[rspec.matching] Running:  $specs"
be rspec $specs
