#!/bin/bash
# Stolen from rwxrob:  github.com/rwxrob/dot

buf="$*"
if test -n "$buf"; then
  tmux -L stream send -t 1 "$buf" Enter
  exit
fi

IFS=
while read -r line; do
  tmux -L stream send -t 1 "$line" Enter
done
