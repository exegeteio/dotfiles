#!/usr/bin/env bash

source=$(mirror_displays display_port)
hdmi=$(mirror_displays hdmi)
capture=$(mirror_displays capture)
if [ -n "$source" ]; then
  mirror -off
  [ -n "$hdmi" ] && mirror -l $source $hdmi
  [ -n "$capture" ] && mirror -l $source $capture
else
  echo "Source not found!"
fi
