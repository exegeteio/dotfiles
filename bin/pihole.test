#!/usr/bin/env bash

piholes="192.168.86.2 192.168.86.3"

for pihole in $piholes
do
  echo "=== $pihole"
  host ads.google.com "$pihole" | grep -i 'has address'
  host www.google.com "$pihole" | grep -i 'has address'
done

