#!/bin/bash
if [[ "$TERM_PROGRAM" == "vscode" ]]; then
  code -r $*
elif [[ -n $TMUX ]]; then
  vim $*
elif [[ -n $SSH_CONNECTION ]]; then
  vim $*
elif [[ -n $DOCKER ]]; then
  vim $*
else
  code $*
fi
