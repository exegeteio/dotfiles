#!/usr/bin/env bash

source=$(mirror_displays capture)
hdmi=$(mirror_displays hdmi)
dp=$(mirror_displays display_port)
if [ -n "$source" ]; then
  mirror -off
  [ -n "$hdmi" ] && mirror -l $source $hdmi
  [ -n "$dp" ] && mirror -l $source $dp
else
  echo "Source not found!"
fi
