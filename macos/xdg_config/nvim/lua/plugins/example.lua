-- Add plugin specs here. Every file in this directory is loaded
-- automatically by lazy.nvim's `{ import = "plugins" }` in
-- lua/config/lazy.lua. See https://lazyvim.github.io/configuration/plugins.
return {}
